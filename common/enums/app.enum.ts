export enum OS {
  ANDROID = 'android',
  IOS = 'iOS',
  LINUX = 'linux',
  MACOS = 'macos',
  OTHER = 'other',
  WINDOWS = 'windows',
}

export enum PartnerLoginSource {
  JIO = 'jio',
  STC = 'stc',
}

export enum LoginType {
  AUTH = 'auth',
  SSO = 'sso',
}

export enum Platform {
  APP = 'app',
  TV = 'tv',
  WEB = 'web',
}

export enum Lang {
  EN = 'en',
  HIN = 'hin',
}

export enum Dialect {
  BHO = 'bho',
  HAR = 'har',
  RAJ = 'raj',
}

export enum StatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum CurrencyEnum {
  INR = 'INR',
  USD = 'USD',
}

export enum CurrencySymbolEnum {
  INR = '₹',
  USD = '$',
}

export enum ClientAppIdEnum {
  ANDROID_HAR_MINI = 'in.stage.har',
  ANDROID_MAIN = 'in.stage',
  ANDROID_RAJ_MINI = 'in.stage.raj',
  IOS_MAIN = 'in.stageflutter',
  WEB = 'in.stage.web',
}

export enum GenderEnum {
  FEMALE = 'female',
  MALE = 'male',
  OTHER = 'other',
}

export enum ProfileStatus {
  ACTIVE = 'active',
  DELETED = 'deleted',
}
